#!/usr/bin/env python3
"""Optional web UI is not bundled in this minimal build.

Use `alma-bulk status` for CLI dashboard output.
"""

if __name__ == "__main__":
    print("No web UI in this minimal build. Use `alma-bulk status`.")
